"""Storage backends for MemAssist memory tiers.

- ``sqlite.py``  — core memory (editable blocks) + recall memory (message log)
- ``chroma.py``  — archival memory (vector store with semantic search)
"""
