# Phase 1 (MVP) runtime dependencies — keep in sync with pyproject.toml
openai>=1.40          # OpenAI-compatible client for all router providers
pyyaml>=6.0           # llm/providers.yaml
langgraph>=0.2        # graph/ owns control flow only — never provider calls
langgraph-checkpoint-postgres>=2.0  # durable turn state; paired with the pg backend
psycopg-pool>=3.2     # the checkpointer's connection pool (turns run off-thread)
langfuse>=3.0        # Phase 5 tracing; inert unless LANGFUSE_* keys are set
langchain-mcp-adapters>=0.3   # external MCP servers as tools (mcp_servers.yaml)
mcp>=1.29,<2          # PINNED: adapters 0.3.1 import RequestContext, dropped in mcp 2.0
chromadb>=0.5
psycopg[binary]>=3.1  # Postgres driver (Phase 4 backend)
pgvector>=0.2         # vector(384) type adapter for psycopg
sentence-transformers>=2.7  # local bge-small-en-v1.5 embeddings (384-d), no API key
fastapi>=0.110         # api/ service (SSE)
uvicorn[standard]>=0.27
pydantic>=2.5
python-dotenv>=1.0

# Dev / test / CI (ci.yml installs this file and runs all three)
pytest>=7.4
pytest-timeout>=2.3   # a hung test must fail by name, not stall the job
ruff>=0.6             # lint gate
pip-audit>=2.7        # dependency audit gate
